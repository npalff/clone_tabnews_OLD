# clone_tabnews
Web development of a clone portal (https://www.tabnews.com.br) following it E2E. 


# Main commands to run the component

In the terminal run the following command to run the project locally.

```
    npm run <script name> (scripts from /package.json)
    e.g.: npm run dev
```

Click the Antenna icon at the bottom to open the **PORTS tab** (it's a shortcut you can also navigate to this tab next to the terminal tab), where it's possible to set the visibility to **Public**. Once enabled, the port will be exposed to the internet, making the test publicly accessible from other networks and devices.